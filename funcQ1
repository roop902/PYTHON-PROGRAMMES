def count_lower_upper(s):
    lower = upper = 0
    for ch in s:
        if ch.islower():
            lower += 1
        elif ch.isupper():
            upper +=1
        return {"lower":lower, "upper":upper}
    print(count_lower_upper("hello there"))


count_lower_upper(s)
